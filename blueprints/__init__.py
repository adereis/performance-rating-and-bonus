"""Flask blueprints for the Performance Rating System.

Each blueprint groups a set of related routes that previously lived in the
monolithic app.py. Blueprints register on the existing module-level ``app``
(see app.py). See docs/REFACTOR_APP_SPLIT.md.
"""
